ΤΣΙΑΤΟΥΡΑΣ ΕΥΑΓΓΕΛΟΣ 
Α.Μ. 1115201200185

Η δομή που έχω υλοποίησει αποτελείται αρχικά από έναν πίνακα ο οποίος είναι το Hashtable και η αρχικοποίση του
πραγματοποιείται κατά την κλήση του προγράμματος χρησιμοποιώντας το flag -b ακολουθούμενο απο το επιθυμητό
μέγεθος. Κάθε στοιχείο του Hashtable (bucket) είναι ένας δείκτης όπου κάθε δείκτης δείχνει σε έναν δυναμικό
πίνακα όπου εμπεριέχονται οι αντίστοιχοι κόμβοι που έχουν σαν κοινό στοιχείο το αποτέλεσμα της Hash Function.
Αξίζει να σημειωθεί οτι η Hash Function που έχω χρησιμοποιήσει είναι πολύ απλή και απλά υπολογίζει την εξής 
αριθμητική παράσταση: position = key % numberOfBuckets. Επίσης κάθε κόμβος περιέχει ακόμη δύο δυναμικούς πίνακες
όπου καθέ ένας απο αυτούς αναπαριστά τις ακμές που εισέρχονται η εξέρχονται απο τον συγκεκριμένο κόμβο. Για παράδειγμα
έστω κομβοι Ν1 Ν2 Ν3, με Ν1->Ν2(amount 2) N3->N1(amount 1) τοτε ο πινακας Out του N1 θα εμπεριεχει το κομβο N2 
και το amount 2 ενω αντίστοιχα ο πινακας In το κομβο N3 και το amount 1, παρομοιως για Ν2, Ν3. ΟΛΟΙ οι πίνακες
της δομής (εκτος του hashtable) κάνουν reallocate στο διπλάσιο μέγεθος που είχαν προηγουμένως. Τέλος το πρόγραμμα 
έχει εξεταστεί με επιτυχία σε όλα τα δοθέντα Operation Files.

Compilation γινεται μεσω Makefile. Άπλα γράψτε την εντολή make για compile και make clean για διαγραφή των αντικείμενων αρχείων και του εκτελέσιμου.
